import { PageStub } from "@/components/page-stub";

export default function ArtistsPage() {
  return (
    <PageStub
      title="artists"
      description="Every artist you have ever played — sortable, searchable, with the full arc of each relationship."
    />
  );
}
