import { PageStub } from "@/components/page-stub";

export default function OverviewPage() {
  return (
    <PageStub
      title="overview"
      description="A decade of listening at a glance — totals, top artists, and the long arc of your library."
    />
  );
}
